# 성능 고도화 3단계 — API별 baseline

부하를 걸지 않은 상태(1 VU)에서 엔드포인트별 순수 처리시간을 재는 k6 스크립트다.
계획 문서가 3단계를 4단계(부하 테스트)보다 앞에 둔 이유는, 이 표가 있어야 부하를 얹었을 때
늘어난 시간을 **경합 탓**으로 돌릴 수 있기 때문이다.

## 실행

```bash
k6 run scripts/perf-k6/baseline.js
```

결과는 실행한 디렉터리에 `baseline-summary.md` / `baseline-summary.json`으로 떨어진다.
**같은 이름으로 덮어쓰므로** 보관할 결과는 `results/`로 옮겨 날짜를 붙인다.

| 환경변수 | 기본값 | 설명 |
|---|---|---|
| `BASE_URL` | 운영 EB 주소 | 측정 대상 |
| `READ_USER` | `perf000100` | 읽기 측정 유저. **활성 유저여야 한다**(아래 함정) |
| `WRITE_USER` | `perf099999` | 변경 측정 전용. READ와 반드시 달라야 한다 |
| `READ_ITERATIONS` | 200 | 읽기 반복. **p95를 말하려면 이 값이 필요하다**(아래) |
| `WRITE_ITERATIONS` | 3 | 변경 반복 |
| `WARMUP` | 10 | 기록하지 않는 예열 (READ만) |
| `YEAR_MONTH` | `2026-07` | 앱 시계 고정값 `clock.fixed-date=2026-07-24` 기준의 "이번 달" |

## 밟으면 조용히 틀리는 함정 네 개

전부 실제로 밟았고, 스크립트가 지금은 막고 있다.

### 0. 표본 30개로는 백분위를 말할 수 없다

처음 `READ_ITERATIONS=30`으로 재고 p95·p99를 표에 실었다. 상위 1% 구간의 표본 수를 세면 끝난다.

```
30 × 0.01 = 0.3개
```

**데이터가 없는 구간을 보간으로 지어낸 값**이고, 실측치가 정확히 그 모양이었다 —
p99가 언제나 max 바로 밑에 붙는다. 독립적인 정보가 아니라 max의 그림자다.

p95도 상위 5%에 1.5개뿐이라 사실상 "두 번째로 큰 값"이었다. **판정이 실제로 흔들린 지점이
하나 있다** — `store/keywords`를 p95 277ms로 SLO(300ms) 통과라고 적었는데 여유가 23ms뿐이었다.
N=200에서 266ms로 재확인해 이제 근거가 선다.

→ 기본값을 **200**으로 올리고 **p99는 결과 표에서 뺐다.** N=200에서도 상위 1%는 2개뿐이라
여전히 지어낸 값이다. **꼬리는 max로 본다.**

### 1. 활성 유저 3만 명은 연속 블록이 아니다

`perf000001`은 거래가 **0건**이다. 활성 3만 명은 10만 명 전체에 흩어져 있다.

```
활성   perf000100 · perf030001 · perf100000
비활성 perf000001 · perf000002 · perf000010 · perf001000 · perf010000 · perf029999 · perf050000
```

적재 문서의 "유저당 거래 수 p50 180 / 평균 180.0 / 최대 180"은 **활성 유저 안에서의 분포**다.
비활성 유저로 돌리면 집계 API가 전부 빈 응답을 20ms에 돌려주고, **0행 응답도 HTTP 200이라
에러율 0%로 초록불이 뜬다.** 표만 보면 "리포트 API는 이미 빠르다"로 읽혀 개선 대상에서 빠진다.
→ `setup()`이 리포트 요약이 비어 있으면 중단한다.

### 2. 활성 유저라도 카드 전부에 거래가 있진 않다

월 15건이 카드 3장에 흩어지므로 카드당 5건 근처다. `cards[0]`을 그냥 쓰면 카드 계열이 또 0행을 잰다.
→ `setup()`이 그 달 거래가 가장 많은 카드를 고른다.

### 3. `POST /api/cards/mydata`는 멱등하지 않다

컨트롤러 문서와 달리 호출할 때마다 카드가 늘어난다(한 세션에 2 → 6 → 11장).
`WRITE_USER`의 카드가 실행할 때마다 누적되므로 이 유저를 다른 측정에 재사용하지 않는다.

## READ와 WRITE를 나눠 재는 이유

34개를 한 목록에 넣고 똑같이 N번씩 돌리면 측정이 데이터를 부순다.

| 엔드포인트 | 반복하면 |
|---|---|
| `DELETE /store/keywords/recent` | 검색 이력을 통째로 지운다 → 이후 `GET /store/keywords`가 빈 결과를 잰다 |
| `POST /card` | `UNIQUE(user_id, card_product_id)`에 걸려 2회차부터 전부 실패 |
| `PATCH /user/payment-pin` | PIN이 바뀌어 이후 결제 계열이 전부 실패 |

그래서 READ(N=200, **SLO 판정의 정본**)와 WRITE(N=3, 전용 유저)로 나눈다.
**WRITE는 표본이 작아 p95를 신뢰하지 않는다** — p50과 max만 본다.
위 표의 세 엔드포인트는 아직 어느 쪽에도 넣지 않았다. 넣으려면 회차마다 상태를 되돌리는
`prepare` 훅이 필요하다.

## 결과

`results/`는 `.gitignore` 대상이다 — **수치와 해석의 정본은 노션**이고 저장소에는 도구만 둔다.

| 파일 | 무엇 |
|---|---|
| `baseline-ec2-n200-20260816.{json,md}` | **정본.** VPC 안 k6 EC2, N=200 |
| `baseline-ec2-20260816.json` | 같은 조건의 N=30 판(`p99` 필드가 남아 있는 쪽) |
| `baseline-local-*.{json,md}` | 노트북에서 인터넷 너머로 잰 값 |

노트북 측정은 **왕복 약 10ms가 섞여 있다.** p50 차이는 그 왕복만큼인데
**p95는 그보다 훨씬 크게 벌어진다** — 가정용 회선이 튀는 구간이 하필 꼬리에만 실리기 때문이다.
SLO 판정이 쓰는 값이 정확히 p95라서 정식 표는 같은 VPC 안에서만 받는다. 순위는 두 경우가 같다.
