package com.fitwallet.domain.benefit.dto;

/** {@code benefit_service.scope_type} CHECK 제약 값. */
public enum BenefitScopeType {

    /** 특정 브랜드에 적용되는 혜택. */
    BRAND,

    /** 특정 업종 카테고리에 적용되는 혜택. */
    INDUSTRY
}
