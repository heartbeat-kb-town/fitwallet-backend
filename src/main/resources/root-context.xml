<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                            http://www.springframework.org/schema/beans/spring-beans.xsd
                            http://www.springframework.org/schema/context
                            http://www.springframework.org/schema/context/spring-context.xsd
                            http://www.springframework.org/schema/tx
                            http://www.springframework.org/schema/tx/spring-tx.xsd
                            http://www.springframework.org/schema/task
                            http://www.springframework.org/schema/task/spring-task.xsd">

    <!--
        프로파일 선택: -Denv=local | prod (미지정 시 local)
        prod 프로퍼티는 값 자리에 ${DB_URL} 같은 환경변수 참조만 두므로,
        환경변수가 없으면 기동 단계에서 실패한다. 운영 비밀번호가 저장소에 남지 않는다.
    -->
    <context:property-placeholder
            location="classpath:config/application-#{systemProperties['env'] ?: 'local'}.properties"/>

    <bean id="clock" class="com.fitwallet.global.config.ClockFactory" factory-method="create">
        <constructor-arg value="${clock.fixed-date:}"/>
    </bean>

    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="jdbcUrl" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
        <property name="maximumPoolSize" value="${jdbc.pool.maximumPoolSize}"/>
        <property name="minimumIdle" value="${jdbc.pool.minimumIdle}"/>
        <property name="connectionTimeout" value="${jdbc.pool.connectionTimeout}"/>
    </bean>

    <!--
        스키마 마이그레이션. 반드시 MyBatis보다 먼저 끝나야 하므로 sqlSessionFactory가
        depends-on으로 이 빈을 기다린다. 실패하면 컨텍스트 기동이 실패한다 (의도된 동작).
    -->
    <bean id="flywayMigrator" class="com.fitwallet.global.config.FlywayMigrator" init-method="migrate">
        <property name="dataSource" ref="dataSource"/>
        <property name="locations" value="${flyway.locations}"/>
        <property name="baselineVersion" value="${flyway.baseline-version}"/>
    </bean>

    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean" depends-on="flywayMigrator">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath:mapper/**/*.xml"/>
    </bean>

    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.fitwallet"/>
        <property name="annotationClass" value="org.apache.ibatis.annotations.Mapper"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    </bean>

    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <bean id="passwordEncoder"
          class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!--
        @Scheduled를 켠다. 현재 유일한 사용처는 DefaultStoreService.refreshPopularKeywords
        (인기 검색어 사전 집계, V13 참고)다.

        여기(루트 컨텍스트)에 두는 이유는 두 가지다.
        ① 스케줄 대상이 서비스 빈인데 서비스는 아래 component-scan이 루트에서 잡는다.
           웹 컨텍스트(servlet-context.xml)에 두면 스캔 범위가 달라 대상이 없다.
        ② @Transactional과 같은 컨텍스트여야 한다. <tx:annotation-driven>이 여기에만 있어서,
           다른 컨텍스트의 빈에는 트랜잭션 프록시가 안 걸린다(§9). 갱신은 쓰기 트랜잭션이므로
           프록시가 반드시 필요하다.

        pool-size를 지정하지 않아 단일 스레드다. 스케줄 작업이 하나뿐이라 충분하고,
        늘리면 같은 작업이 겹쳐 돌 여지만 생긴다.

        ⚠️ 이 스케줄러는 인스턴스마다 하나씩 돈다. EB가 단일 인스턴스라 지금은 문제가 없지만,
        인스턴스를 늘리면 5분마다 N번 집계된다(결과는 같아 무해하나 낭비이고, 동시에 돌면
        한쪽이 잠깐 락을 기다린다). 그때는 MySQL EVENT나 락 기반 단일화로 옮겨야 한다.
    -->
    <task:annotation-driven scheduler="taskScheduler"/>
    <task:scheduler id="taskScheduler"/>

    <!--
        웹 계층(@Controller, @ControllerAdvice)은 servlet-context.xml이 스캔한다.
        여기서 같이 스캔하면 빈이 두 컨텍스트에 중복 등록된다.

        SwaggerConfig도 같은 이유로 제외한다. @Configuration은 @Component 계열이라 기본 필터에 걸리는데,
        springfox는 RequestMappingHandlerMapping을 읽어야 문서를 만들고 그 빈은 웹 컨텍스트에만 있다.
        여기서 잡히면 핸들러 매핑이 없는 루트 컨텍스트에 springfox 인프라가 한 벌 더 생겨
        문서가 비거나 기동이 깨진다.
    -->
    <context:component-scan base-package="com.fitwallet">
        <context:exclude-filter type="annotation"
                                expression="org.springframework.stereotype.Controller"/>
        <context:exclude-filter type="annotation"
                                expression="org.springframework.web.bind.annotation.ControllerAdvice"/>
        <context:exclude-filter type="assignable"
                                expression="com.fitwallet.global.config.SwaggerConfig"/>
    </context:component-scan>

</beans>
